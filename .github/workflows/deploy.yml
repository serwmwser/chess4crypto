name: Deploy to GitHub Pages

on:
  push:
    branches: [ main ]
  workflow_dispatch:

# ✅ Разрешаем деплой на GitHub Pages
permissions:
  contents: read
  pages: write
  id-token: write

# ✅ Одна задача для деплоя
concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build-and-deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    
    steps:
      # 1️⃣ Checkout кода
      - name: Checkout
        uses: actions/checkout@v4

      # 2️⃣ Настройка Node.js (используем версию 18, как у вас локально)
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18.20.8'
          cache: 'npm'
          cache-dependency-path: 'chess4crypto/frontend/package-lock.json'

      # 3️⃣ Настройка GitHub Pages
      - name: Setup Pages
        uses: actions/configure-pages@v4

      # 4️⃣ Переход в папку frontend и установка зависимостей
      - name: Install dependencies
        run: |
          cd chess4crypto/frontend
          npm ci --ignore-scripts --prefer-offline

      # 5️⃣ Сборка проекта (используем npx для надёжности)
      - name: Build
        run: |
          cd chess4crypto/frontend
          npx vite build --emptyOutDir

      # 6️⃣ Загрузка артефактов
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: 'chess4crypto/frontend/dist'

      # 7️⃣ Деплой на GitHub Pages
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4