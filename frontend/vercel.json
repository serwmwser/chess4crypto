{
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ],
  "env": {
    "NPM_FLAGS": "--legacy-peer-deps --ignore-scripts"
  },
  "build": {
    "env": {
      "NODE_VERSION": "24"
    }
  }
}